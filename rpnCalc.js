createStack(){
  let stack = [];

  push(element){
    stack.push(element);
  };

  pop(){
    stack.pop();
  };

  peek(){
    return stack[stack.length-1];
  };
}

rpnCalc(var1, var2, operator){
  let stack = [];
  let var1 = '';
  let var2 = '';
  let operator = + || - || / || *;

  if (stack = [])
    
  stack.push(var1)


}
